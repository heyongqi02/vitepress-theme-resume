/**
 * CSS build dependencies referenced from {@link ./index.css}. Keeps these packages visible to the
 * pack dependency scanner.
 */
export const styleDependencies = ["tailwindcss", "@tailwindcss/vite"] as const;
